Index PAG
<div class="album py-5">
  <div class="container-fluid">
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-4 g-3" id="profile_lands">

      <% @lands.each do |profile_land| %>
        <%= render "profile_land", land: profile_land %> 
      <% end %>

    </div>
  </div>
</div>
